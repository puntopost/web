url: "https://www.puntopost.mx"

# URL del script de embed del registro de merchants (producción).
# Override local en _config_dev.yml (ver `make server`).
embed_url: "https://merchant.puntopost.mx/embed.js"

exclude:
  - Makefile
  - nginx.conf
  - CLAUDE.md
  - README.md
  - vendor
  - node_modules
  - _config_dev.yml
